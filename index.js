module.exports = {
    plus: (first, second) => first + second ,
    subtract: (first, second) => first - second,
    multipy: (first, second) => first * second,
    divide: (first, second) => first / second,
}